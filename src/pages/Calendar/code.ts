const calendar = {
  collapse: `<Calendar />`,
  expand: `
import Calendar from "@packer-ui/Calendar";

const CalendarComponent = () => {
  //
  return <Calendar />;
};

export default CalendarComponent;
`,
};

export { calendar };
